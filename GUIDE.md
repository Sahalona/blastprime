# BlastPrime Studio 重构规范文档 (GUIDE.md)

> 本文档是「BlastPrime Studio」的完整规范操作手册。
> 本文档只描述**用户可见的功能、操作流程、文件读写格式、可视化与行为规则**,不含内部代码实现细节。
> 一切行为以此文档为准。

---

## 目录

1. [项目概述与架构](#1-项目概述与架构)
2. [运行、分发与依赖管理](#2-运行分发与依赖管理)
3. [全局界面与交互规范](#3-全局界面与交互规范)
4. [模块一:本地 BLAST 数据库构建与管理](#4-模块一本地-blast-数据库构建与管理)
5. [模块二:BLAST 比对与可视化(独立使用)](#5-模块二blast-比对与可视化独立使用)
6. [模块三:短序列 / 引物分析](#6-模块三短序列--引物分析)
7. [模块四:引物设计](#7-模块四引物设计)
8. [引物设计的可视化联动](#8-引物设计的可视化联动)
9. [文件读写格式规范](#9-文件读写格式规范)
10. [配置与状态持久化](#10-配置与状态持久化)
11. [打包与分发规范(PyInstaller)](#11-打包与分发规范pyinstaller)
12. [错误处理与用户提示规范](#12-错误处理与用户提示规范)
13. [验收清单](#13-验收清单)

---

## 1. 项目概述与架构

### 1.1 项目定位

BlastPrime Studio 是一款面向生物信息学本地检索与实验设计的桌面级应用,包含三大能力:

1. **本地 BLAST 数据库构建与管理**:将私有基因组、转录组、scaffold 或自定义 FASTA 集合构建为可检索的本地数据库,数据不离开用户机器。
2. **BLAST 比对与可视化**:对本地数据库执行 BLAST 检索,并以交互式图形方式呈现结果;此模块可**完全独立使用**,不依赖引物设计。
3. **引物设计(基因组特异引物)**:以整段序列的基因组比对结果为依据,通过"特异性剖面四段式分级 + primer3-py + 逆向特异性评估"流程设计高特异性的 PCR 引物,并将所有中间证据可视化。

**功能权重:** 引物设计与 BLAST 可视化并列为核心功能(双主入口)。BLAST 比对与可视化是独立的一级功能,引物设计是构建在其上的高级应用——用户只做 BLAST 检索时,界面不应出现任何引物设计流程的依赖或引导。

### 1.2 架构总则

- **架构形态**:后端服务 + 本地开端口 + 自动打开浏览器。
  - 后端使用 Python 编写的 FastAPI 应用,由 uvicorn 启动,监听 `127.0.0.1` 回环地址(仅本机可访问)。
  - 启动时自动打开系统默认浏览器访问该端口;在无桌面环境的系统(如纯服务器、无 DISPLAY 的 WSL)上不打开浏览器,仅打印访问地址。
  - 前端为纯静态页面,位于项目 `static/` 目录,由 FastAPI 直接托管;前端所有资源(JS/CSS/字体/图表库)必须随程序本地打包,**禁止依赖任何外部 CDN**。
  - 界面语言:简体中文与英文双语,可在设置中切换,默认跟随系统或沿用上次选择。
- **技术栈(按需求指定)**:
  - Python 3.10+(仅 Python,无原生扩展依赖)
  - FastAPI + uvicorn(Web 服务)
  - Biopython(FASTA 解析、序列操作)
  - primer3-py(引物物理参数计算与引物设计任务)
  - 前端:原生 HTML/CSS/JavaScript 或任何可本地打包的轻量方案,不引入框架亦可
  - 环境管理:`uv`(pyproject.toml / uv.lock,推荐但不必须)——也支持标准虚拟环境 + `pip install -r requirements.txt`
- **进程模型**:
  - 所有 BLAST 子进程、设计任务在后台线程/进程执行,不阻塞 HTTP 接口;每个长任务可被用户取消。
  - 任务进度、日志通过 WebSocket 或 SSE 实时推送到前端(必须支持断线重连)。

### 1.3 目录结构(约定)

```
BlastPrimeStudio/
├── pyproject.toml            # uv 项目定义
├── uv.lock
├── blastprime/               # 后端 Python 包(主程序)
│   ├── __init__.py
│   ├── app.py                # FastAPI 应用与路由
│   ├── blast.py              # BLAST 程序定位、建库、检索、结果解析
│   ├── database.py           # 数据库历史记录、配置管理
│   ├── primer_metrics.py     # 引物物理指标(primer3-py 封装)
│   ├── primer_design.py      # 四段式剖面分级设计流程
│   ├── masking.py            # 匹配深度分析、屏蔽掩码
│   ├── spec_eval.py          # blastn-short 逆向特异性评估与排序
│   ├── tasks.py              # 后台任务调度、取消、进度推送
│   └── ...
├── static/                   # 前端页面(HTML/CSS/JS),完全本地
│   ├── index.html
│   ├── blast.html            # BLAST 比对与可视化页
│   ├── design.html           # 引物设计页
│   ├── css/
│   └── js/
├── bin/                      # 随源码分发或打包的 NCBI BLAST+ 可执行文件
│   ├── blastn(.exe)
│   ├── makeblastdb(.exe)
│   ├── blastdbcmd(.exe)
│   └── ...(其余 BLAST+ 程序)
├── local_blast_dbs/          # 程序默认数据库目录(自动创建,含 .gitignore)
│   └── config.json           # 配置与历史记录
└── README.md / README_zh.md
```

---

## 2. 运行、分发与依赖管理

### 2.1 源码运行(Linux / macOS / Windows)

**方式一:uv(推荐,但不是必须)**——安装 `uv`(https://docs.astral.sh/uv/),项目根目录执行 `uv sync` 安装依赖,`uv run blastprime` 启动。

**方式二:标准 Python 虚拟环境 + pip**:

1. `python3 -m venv .venv` 创建虚拟环境并激活(`source .venv/bin/activate`,Windows 为 `.venv\Scripts\activate`);
2. `pip install -r requirements.txt` 安装依赖(依赖清单与 pyproject.toml 同步维护);
3. `python -m blastprime.app` 启动(入口脚本 `blastprime` 在 pip 安装后同样可用)。
4. 启动后自动打开浏览器;若未打开,手动访问终端打印的地址(默认 `http://127.0.0.1:8686`)。

### 2.2 命令行参数

| 参数 | 说明 | 默认 |
|---|---|---|
| `--host` | 监听地址 | `127.0.0.1` |
| `--port` | 监听端口;若被占用则自动顺延,并在日志中告知实际端口 | `8686` |
| `--no-browser` | 不自动打开浏览器 | 自动打开 |
| `--loglevel` | 日志级别:`DEBUG` / `INFO` / `WARNING` / `ERROR` | 配置值,缺省 `INFO` |
| `--logfile <path>` | 将日志同时写入指定文件 | 配置值,缺省不写文件 |
| `--config <path>` | 自定义配置文件路径 | `local_blast_dbs/config.json` |

### 2.3 NCBI BLAST+ 程序定位规则(关键)

程序在任何操作执行前,按**固定顺序**寻找 BLAST+ 可执行文件(`blastn`、`makeblastdb`、`blastdbcmd` 等):

1. **用户手动指定的 `blast_bin_dir`**(持久化配置)优先于一切候选(代码在候选列表头部插入)。
2. **打包模式(Windows .exe)**:依次查找 `sys._MEIPASS/bin/`(PyInstaller 解包目录)、可执行文件同级目录的 `bin/`。
3. **源码/未打包模式**:依次查找项目根目录、项目根目录下 `bin/`、系统 `PATH` 环境变量。
4. 找到的每个工具路径会缓存在内存中,并在设置页展示"环境检测"结果。

**找不到时的处理**(仅未打包模式需要):

- 在界面顶部显示醒目横幅警告,列出缺失的工具名;
- 提供两个按钮:
  - **"打开安装说明"**:展示图文指引(NCBI 官方下载地址、conda/apt/brew 安装命令);
  - **"手动指定 BLAST 目录"**:让用户通过文件选择器指定包含 BLAST 可执行文件的 `bin` 目录,指定后立即重新检测并持久化到配置;
- 在警告未消除前,所有依赖 BLAST 的操作(建库、比对、引物设计)的按钮置灰并提示原因。

> 打包模式中 `bin/` 由 PyInstaller 随程序携带,不提示安装;但若 `bin/` 缺失或损坏(例如被杀毒软件误删),仍按同一规则降级检测并提示。

### 2.4 自动打开浏览器的判定

- Windows:直接打开默认浏览器。
- macOS:`open` 调用。
- Linux:检测 `DISPLAY` / `WAYLAND_DISPLAY` 环境变量存在时打开;不存在(纯命令行环境)则只打印 URL,并保持服务运行直到用户 Ctrl+C 退出。

---

## 3. 全局界面与交互规范

### 3.1 页面骨架

所有页面统一骨架:

- **顶部导航栏**:应用名称 "BlastPrime Studio"、主导航(① BLAST 比对 / ② 引物设计)、设置入口、语言切换、环境状态灯(绿=就绪,红=BLAST 缺失)。
- **主内容区**:当前模块页面。
- **底部状态栏**:当前任务状态(就绪 / 运行中 / 已完成 / 失败)、进度条、取消按钮(任务运行时出现)。
- **全局日志抽屉**:可从任何页面打开的实时日志面板(分页/滚动、按级别着色、支持搜索)。

### 3.2 通用交互规则

- **任务运行中**:发起任务的按钮变为"停止"样式;停止后立即终止 BLAST 子进程并清理临时文件,状态栏显示"已取消"。
- **所有数值输入**均做即时合法性校验;非法时输入框红框提示并阻止提交,给出中文/英文错误说明。
- **所有表格**支持列排序(点击表头,再次点击反转)、文本过滤框。
- **所有序列类文本**使用等宽字体展示;较长的输出(比对文本、原始输出)放在可滚动的等宽文本区域。
- **复制**:所有序列、比对文本、引物序列均有"一键复制"按钮,复制后给出轻提示。
- **双语言**:所有界面文案走 i18n 表(中/英),切换后整个界面即时刷新;序列内容、日志内容不做翻译。

### 3.3 主题

- 提供浅色 / 深色两种主题,默认跟随系统,可在设置中覆盖并持久化。
- 所有可视化(图形摘要、深度曲线、比对着色)必须同时适配两种主题。

---

## 4. 模块一:本地 BLAST 数据库构建与管理

### 4.1 功能清单

| # | 功能 | 说明 |
|---|---|---|
| 1 | 构建数据库 | 从一个或多个 FASTA 文件构建核酸(nucl)或蛋白(prot)数据库 |
| 2 | 序列类型自动检测 | 自动模式:按样本序列字符构成判定核酸/蛋白(核酸字符占比 ≥90% 判为核酸) |
| 3 | 多文件合并 | 选择多个 FASTA 时自动合并为一个输入再建库,不修改原文件 |
| 4 | 默认存储 | 未指定输出目录时,存到程序数据目录 `local_blast_dbs/blastdb_<随机后缀>/`,每个库独立目录,重建不覆盖旧库 |
| 5 | 指定输出目录 | 用户可自选输出目录(此时使用给定目录 + 库名) |
| 6 | 实时建库日志 | 建库过程的日志流实时显示在页面上(含 makeblastdb 标准错误) |
| 7 | 库详情 | 对当前库执行 `blastdbcmd -info`,弹窗展示序列数、总长度、类型等 |
| 8 | 历史记录 | 记录所有使用过的库(路径 + 是否由本程序创建),可一键切换为当前库 |
| 9 | 浏览已有库 | 手动选择磁盘上已有的 BLAST 索引文件(`*.nin`/`*.pin`)并加入历史 |
| 10 | 删除历史记录 | 两种删除方式:仅移除记录(保留文件)/ 连同索引文件物理删除(仅限本程序创建的库) |
| 11 | 失效记录清理 | 载入配置时自动剔除索引文件已不存在的历史条目 |
| 12 | 环境检测 | 显示 makeblastdb / blastn / blastdbcmd 等工具路径或"未找到" |

### 4.2 操作流程

1. 进入"数据库"页面(可从顶部导航进入,建库是 BLAST 比对与引物设计的前置能力,但页面本身独立)。
2. **选择 FASTA 文件**(可多选,支持 `*.fasta *.fa *.fna *.faa *.txt`;多选时界面显示全部路径,分号分隔)。
3. 选择**序列类型**:自动检测 / 核酸 / 蛋白。
4. 填写**数据库名称**(缺省 `mydb`,仅字母数字下划线)。
5. (可选)填写**输出目录**;留空则使用默认存储。
6. 点击"构建数据库"。
   - 校验:BLAST 工具存在;文件存在;文件可被 FASTA 解析。
   - 自动检测时若无法判定类型,报错并让用户手动指定。
7. 完成后:
   - 状态栏与日志显示成功;
   - 该库自动成为"当前数据库",写入历史记录;
   - 界面显示库前缀路径与类型;
   - 可一键跳转到 BLAST 比对页继续使用。

### 4.3 建库行为规格

- 使用 NCBI `makeblastdb`,必须带 `-parse_seqids`(保证 blastdbcmd 可按条目标识访问,支持名称型 query)。
- 超时 300 秒;超时按失败处理并给出明确错误。
- 构建失败时,弹窗展示 makeblastdb 的错误输出(不隐藏 stderr 内容)。
- 建库是后台任务,期间界面可继续操作其他页面。

### 4.4 数据库历史记录

- 记录字段:`prefix`(库前缀路径)、`is_created`(是否由本程序创建)。
- 历史下拉框显示库名(程序创建的库名追加 `[由本程序创建]` 标记)。
- 物理删除:任意来源的库均可物理删除(前端对每行记录提供"删除文件"确认);删除时移除全部索引文件(`.nin/.nsq/.nhr` 及蛋白对应 `.pin/.psq/.phr`,及 `.nsd .nsi .nnd .nni .njs .nog .pog .psd .psi .pnd .pni .pjs` 等),若父目录是本程序创建的 `blastdb_*` 空目录则一并删除;外部目录库只删成套索引、不删父目录;操作前必须弹确认框,明示"将物理删除磁盘文件"。

---

## 5. 模块二:BLAST 比对与可视化(独立使用)

> 本模块是**一级独立功能**:不依赖、也不引导引物设计。用户仅做序列检索时,界面上不出现任何引物设计入口。引物设计页内部复用的"位点定位 BLAST"与"特异性 BLAST"属于内部能力,不影响本模块的独立性。

### 5.1 功能清单

| # | 功能 | 说明 |
|---|---|---|
| 1 | 选择数据库 | 当前库 / 历史下拉 / 浏览已有库三种方式;也支持 NCBI 远程库 |
| 2 | 查询输入 | 文本框直接粘贴 FASTA;或从文件导入 FASTA |
| 3 | 名称型 query | 直接输入数据库条目名即可检索(详见 5.3),支持 `range=` 与 `database=` 选项 |
| 4 | 序列工具 | 反向互补、序列清洗(剔除非法字符)、6 框翻译(可选遗传密码子表) |
| 5 | 程序自动选择 | 按(库类型 × 查询类型)自动匹配 blastn/tblastn/blastx/blastp,也支持手动指定 |
| 6 | 参数设置 | E-value、最大比对目标数(页面未提供得分矩阵/附加参数控件,后端预留) |
| 7 | 短序列/引物模式 | 针对 15~30 bp 查询优化(自动改用 blastn-short / blastp-short 与更小 word_size,放宽 evalue) |
| 8 | NCBI 远程比对 | 可选;需填写远程库名(nr/nt 等);发送前必须弹窗警告数据将离开本机且可能耗时数小时 |
| 9 | 运行/停止 | 一键开始与停止;停止立即杀掉 BLAST 进程 |
| 10 | 结果树 | Query → Subject → HSP 三级树,可排序(自然序)、可过滤 |
| 11 | 图形化摘要 | 以查询坐标轴的条带图展示全部 HSP,支持缩放/平移/悬停提示/点击联动 |
| 12 | 比对详情 | 逐条 HSP 的成对比对文本,错配/匹配高亮可切换,支持一键复制目标序列 |
| 13 | 原始输出 | 完整展示 `-outfmt 0` 文本;大文件分批加载(每批 1000 行,"加载更多"按钮) |
| 14 | 导入原始输出 | 载入已有 `-outfmt 0` 文本文件,直接可视化,无需重跑 BLAST |
| 15 | 导出 | 原始输出(.txt)、当前比对(.txt)、结果统计表(.csv)、命中序列(FASTA) |
| 16 | 项目保存/加载 | 将当前查询、参数、结果整体存为项目文件并可随时恢复(见第 9 章) |

### 5.2 比对参数

| 参数 | 默认 | 说明 |
|---|---|---|
| BLAST 程序 | 自动 | 可选 auto、blastn、blastp、blastx、tblastn(无 tblastx) |
| E-value 阈值 | 10 | 越小越严格,支持科学计数法(如 1e-5) |
| 最大比对目标数 | 500 | 限制返回的 subject 数量 |
| 得分矩阵 / 附加参数 | 后端预留 | 页面未提供控件;矩阵默认 BLOSUM62(蛋白) |

**程序自动选择规则**:

| 库类型 \ 查询类型 | 核酸 | 蛋白 |
|---|---|---|
| 核酸库 | blastn | tblastn |
| 蛋白库 | blastx | blastp |

**短序列/引物模式行为**:

- 勾选后:blastn 使用 `-task blastn-short -word_size 7`,blastp 使用 `-task blastp-short -word_size 2`,E-value 阈值自动放宽到 1000。
- 若查询序列 ≤30 bp 而未勾选,提交前弹窗警告"短序列在普通模式下可能无结果,是否继续?"。
- 勾选该模式后,结果页出现"引物分析"标签(见模块三)。

**名称型 query 语法**(FASTA 标题,大小写不敏感选项):

```
>gene_name
>gene_name,range=100-200
>gene_name,range=100-*,database=my_db_basename
>gene_name,range=*-200,database=basename
```

- 名称可附带可选参数,逗号分隔:`range=起始-结束`(支持 `*` 通配)、`database=库basename`(按历史记录中的库名匹配)。
- 解析流程:按库条目列表(经 blastdbcmd 列出)对名称做精确 → 去 `lcl|` 前缀 → 模糊包含匹配;取到库序列后自动按范围切片,并回填为普通 FASTA 再执行 BLAST。
- 名称解析失败时给出明确错误(未找到数据库 / 未选择数据库 / 库中找不到该基因)。
- 远程模式:名称型 query 仍从本地库解析回填为 FASTA 后发送远程比对(仅 targetbase 指定的运行库被忽略)。

### 5.3 查询输入校验

- 非法字符(数字、空格混入序列行)在提交时被检出,提示先执行"序列清洗";
- 序列清洗仅保留字母、`*`、`-`,并保留 FASTA 标题行;
- 反向互补对序列行做 A↔T、C↔G(U↔A)的反向互补,标题行追加 `(rev_comp)` 标记;
- 6 框翻译:对每条 DNA 序列按所选遗传密码表(标准表及其余常用表,含线粒体各表等)做正链 3 框与负链 3 框翻译,弹窗展示 6 段蛋白序列,支持切换序列、切换密码表、一键复制全部。

### 5.4 结果展示与交互

#### 5.4.1 结果树

- 层级:`Query(查询名,长度)` → `Subject(库条目)` → `HSP n`。
- 列:Identity(如 `245/250 (98%)`)、E-value、Score(bits)、Length。
- Subject 行显示该条目最佳 HSP 的数值;HSP 行显示自身数值。
- 表头点击排序(整数/百分比自然排序);顶部过滤框按 query 名与 subject 名即时过滤。
- 载入结果后自动展开所有 query 并选中第一个,右侧面板立即有内容。

#### 5.4.2 图形化摘要(可视化核心)

- 画布上部为固定**标尺头部**:显示 query 名称、长度、当前缩放百分比,0~100% 等分刻度线。
- 主画布按**查询坐标**横向绘制每个 HSP 条带(bar):位置对应 `q_start`~`q_end`,宽度按查询长度归一。
- 条带颜色按 Score(bits)分级:≥200 红、≥80 品红、≥50 绿、≥40 蓝、其余黑。
- 交互:
  - **悬停**显示浮动提示(Sbjct、Score、E-value、Range);
  - **点击条带**选中对应 HSP,联动结果树与比对详情;
  - **Ctrl+滚轮**(及 Linux 的 Ctrl+Button4/5)横向缩放;缩放后标尺与条带同步;
  - 鼠标拖拽/滚动条横向平移;纵向滚动浏览多行;
  - 选中的条带以加粗描边(品红)高亮;点击结果树中的条目时,对应条带高亮。
- 当查询无命中时,画布显示"无命中"占位。

#### 5.4.3 比对详情

- 展示选中 HSP 的成对比对文本(Query/Sbjct 三行式),等宽字体。
- 高亮模式二选一:**错配/缺口高亮**(默认)或**完全一致高亮**,切换即时刷新。
- "复制 Sbjct 序列"按钮:从比对文本中提取目标序列纯净片段(含反向),复制到剪贴板。
- 选中 Subject 节点时聚合展示其全部 HSP 比对;选中 Query 节点时聚合全部。

#### 5.4.4 原始输出与导入

- 原始输出页完整保留 BLAST 的 `-outfmt 0` 文本;大文本分批加载(每批 1000 行,点击"加载更多"按钮追加),保证流畅。
- **导入原始输出**:选择 `.txt/.out/.blast` 文件,校验包含 `Query=` 且为成对输出格式,解析后填充结果树/图形/比对页;导入后短序列模式开启且为核酸时显示"引物分析"标签(默认停在比对结果页,可手动切换)。

#### 5.4.5 导出格式

- **原始输出 .txt**:当前完整 `-outfmt 0` 文本。
- **比对 .txt**:当前选中内容的纯文本。
- **结果统计表 .csv**(UTF-8 with BOM,Excel 兼容),列:

  ```
  Query_ID, Subject_ID, Identity, E-value, Bit_Score, Subject_Length, Query_Start, Query_End
  ```

- **命中序列 .fasta**:每个 HSP 的目标片段一条记录,标题 `>subject_id_hspN [from_query:xxx score:xxx]`,60 字符换行。

### 5.5 远程比对

- 勾选后隐藏本地库选择,改为填写远程库名(默认 `nr`)。
- 提交前必须弹出警告:"序列将发送至 NCBI 服务器,可能排队数分钟至数小时"。
- 远程模式不使用本地 BLAST 程序定位(仍需 blastn 本体),输出格式与本地一致,全部下游功能(可视化/导出/分析)照常可用。

---

## 6. 模块三:短序列 / 引物分析

> 适用场景:用户在 BLAST 比对中查询的是单条引物或短序列(≤100 bp,典型 15~30 bp)。该模块自动出现在结果页,对当前选中的查询序列做引物物理指标与特异性评估。

### 6.1 触发条件

- 勾选了"短序列/引物模式",且比对结果中所有查询长度 ≤100 bp;
- 引物设计任务完成后,对每条设计的引物/引物对同样复用本评估逻辑。

### 6.2 报告内容

对选中序列(视为 5'→3' 引物)逐项评估并输出带颜色等级的指标表:

| 指标 | 良好(绿) | 可接受(黄) | 差(红) |
|---|---|---|---|
| GC 含量 | 40%~60% | 30%~40% 或 60%~70% | <30% 或 >70% |
| Tm 值 | 50~65°C | 45~50°C 或 65~70°C | <45°C 或 >70°C |
| 3' 端 G/C 数(GC 夹子,末 5 bp) | 1~3 | 0 或 4 | ≥5 |
| 自互补二聚体(最大连续配对) | ≤3 bp | 4 bp | ≥5 bp |

- Tm 计算规则:短序列(<14 bp)用 `2×(A+T)+4×(G+C)` 法则;较长序列用盐校正公式;若采用 primer3 物理模型,则以 primer3 的 Tm 为准并在界面注明算法来源。
- 二聚体评估:计算序列与其反向互补序列在所有位移下的碱基互补配对,输出"最大连续配对长度"与"最大总配对长度"。
- **BLAST 特异性评估**(依赖当前比对结果):
  - 无任何命中 → "库中未找到匹配靶标"(红);
  - 命中首个 HSP 外的其余 HSP 按"有效匹配长度 `L` 相对 3' 端偏移 `k` 与错配数 `errors`"判定:
    - `L ≥ 高危阈值(默认 8)+ 2k + 3×errors` 且 E-value ≤10 → **高非特异性风险**(3' 端可能结合,红);
    - `L ≥ max(8, 阈值-2) + 2k + 3×errors` 且 E-value ≤10 → **潜在非特异风险**(黄);
  - 仅命中 1 个目标 → "特异性高"(绿);命中目标内多处(同一库条目多个 HSP)→ "匹配单个基因但存在多处结合区"(黄)。
- **总体判定**:任一红 → 不可用;无红但有黄 → 风险/待定;全绿 → 可用。
- 输出"判定理由"逐条列出违规项与达标项。

### 6.3 展示形式

- 指标卡片式布局(绿/黄/红圆点 + 说明),附"可用性判定"大字结论;
- 特异性命中列表可展开查看每条 HSP 的比对详情与 3' 端结合情况;
- 提供"复制报告"按钮,以纯文本复制完整报告。

---

## 7. 模块四:引物设计

> 本模块是核心新功能。核心思想:**先让 BLAST 数据说话(全序列基因组比对 → 逐碱基匹配深度 → 特异性剖面),再以四段式分级配合 primer3-py 出候选,最后以 blastn-short 逆向评估特异性**。

### 7.1 设计输入

用户在引物设计页填写/选择:

1. **模板序列来源**(二选一):
   - **A. 输入序列**:在文本框粘贴 FASTA(侧翼序列或目标区域序列),通常为一条包含目标位点及上下游的 DNA 序列;
   - **B. 名称+范围**:直接引用已选本地库中的条目(复用名称型 query 语法,如 `>gene,range=1000-2500`),由程序取库序列作为模板。
2. **本地基因组数据库**:必选(核酸库)。这是匹配深度分析与特异性评估的参考基因组。第一步整段模板 blastn 不可关闭,但可勾选两个跳过开关:**跳过特异性查询**(skip_spec_eval,跳过 blastn-short 逆向验证,特异性分按 k-mer 剖面)与**跳过 k-mer 评分过程**(skip_kmer_scoring,不建深度/剖面,四段式按全放行设计,不查/不写 k-mer 缓存,特异性分 0 并注明)。
3. **目标区域**(可选):若用户指定了目标区域(在模板序列上的起止坐标),产物必须覆盖该区域;不指定时,全序列皆可作为目标。
4. **设计参数**(详见 7.6)。

### 7.2 第一步:全序列 BLASTn 基因组比对与匹配深度分析

- 将**整段模板序列**作为查询,对所选基因组数据库执行 blastn(非 short 模式;E-value 阈值放宽,默认 10;`max_target_seqs` 放大到足以覆盖所有重复命中,默认 5000),使用带坐标的 tabular 输出(包含 qstart/qend/sstart/send/pident/evalue/bitscore/qseq/sseq/sstrand)。
- **逐碱基匹配深度分析**:窗口 k-mer 出现次数由 kmer_count 纯 Python 计数程序给出(8/10/12/15-mer 逐碱基窗口,步长 1,单次 blastn-short 之外的精确计数;R12 起窗口查询不再走 blastn),计算每个模板碱基位置 k-mer 的基因组出现次数,得深度向量 `d(i)`(同尺度同条目相邻位点合并,跨条目累计);
  - 深度 ≥2 的位置标记为"多重匹配/重复区",深度 =1 的位置标记为"单拷贝区",深度 =0 的位置(无匹配)标记为"无匹配区";
  - 深度分布统计:全序列匹配率、重复区占比、各深度直方图。
- 分析结果**立即可视化**(见第 8 章):特异性剖面色带、global/3' 双曲线、目标区标记。

### 7.3 四段式剖面分级设计(Level 1 ~ Level 4)

**特异性剖面**:以 7.2 的深度向量为基础,每位置得分 `S = count^(-1/3)`(幂函数;count≤1(缺席/唯一)→ 1.0,count=2 → 0.794,count=8 → 0.5,count 越大分越低——R32 起替代旧式 1/(1+log2(count)),后者在整数 count 下分数两极分化、过渡区不可达);`global` 剖面 = 四尺度(8/10/12/15-mer)得分的逐位置 max 合并("最强特异性证据"),`three_prime` 剖面 = 各尺度以该位置结尾的 k-mer 得分取 max;模板内自重复分量(仅模板索引)与深度分逐碱基取 min 合并(只加强掩蔽)。

**可用区**:某级下 `global ≥ g 且 three_prime ≥ t` 的连续位置。四段式按 **count 分档**逐级放宽(R33):count 为位置 k-mer 的出现次数(四尺度 max 合并后取其档位):

| 级别 | global 阈值 | three_prime 阈值 | count 档位 | 说明 |
|---|---|---|---|---|
| Level 1 | 1.0 | 1.0 | count=1 | 严格(唯一) |
| Level 2 | 0.6934 | 0.6934 | count 2-3 | 中等 |
| Level 3 | 0.5503 | 0.5503 | count 4-6 | 放宽 |
| Level 4 | —(None) | —(None) | count≥7 | 无预屏蔽,全放行 |

- **L2/L3 阈值可配置**:高级参数"阶段二/三放行阈值"(level2_global_th / level3_global_th)可覆盖 L2/L3 的 global/3' 阈值(默认即上表 count 分档值);可视化色带/参考线/图例随设计实际使用的阈值渲染(结果 `level_thresholds` 下发完整四级配置,含未跑到的级别)。
- 某级有产出(幸存引物对 ≥1)即成功并停止;否则进入下一级。
- 每级调用 primer3-py 设计(任务类型:引物对设计;输入为模板序列、排除区约束与 7.6 参数;产物长度、Tm、GC、长度、发夹/二聚体等物理约束全部由 primer3-py 计算与校验);每级候选数上限 `candidate_count`(默认 50),经坐标多样性均衡。

**可用区两侧缓冲(贯穿各阶段,默认 8 bp,可配置)**:

- 每个连续可用区上下游各外扩 buffer_len 作为**设计允许区**(primer3 的 SEQUENCE_EXCLUDED_REGION 只排除扩展边界之外);
- **方向性**:正向引物 5' 端在左(在可用区**左侧**扩展)、反向引物 5' 端在右(在可用区**右侧**扩展)——引物 5' 端可落入非特异缓冲带,3' 端必须留在特异区;
- 两条引物的 **3' 端由级内后置过滤约束在原始可用区**内(`_in_allowed_runs` 检查);缓冲带自身剖面低于阈值,3' 端不可能落于其中;
- 缓冲外扩**裁剪到 F/R 设计范围内**(范围边界处不向范围外扩展,避免引物超出用户规定的区域);
- F/R 设计范围外区域(正、反向引物 3' 端均不可放置的区间补集)并入排除区,并随结果下发 `range_excluded` 供可视化灰标。

**级内后置过滤**(每级 primer3 候选之后):

- 两条引物 3' 端必须落在原始可用区内 + F/R 设计范围内;
- 3' 端 GC 夹子(末 5 bp 最多 3 个)与引物对二聚体(≤5 bp 连续配对)检查;
- **3' seed 深度预筛**:以 3' 端 12-mer 窗口位点深度 > `prefilter_max_hits`(默认 200)为快速淘汰(明显不特异候选);
- 幸存候选进入 7.4 的逆向特异性评估。

### 7.4 逆向特异性评估(blastn-short + 3' 端法则 + in-silico PCR)

每级(含 Level 4 全放行)的幸存候选统一执行:

1. 对候选池中所有引物(正向与反向各作为独立序列)执行 **blastn-short**(短序列模式参数:word_size 7、E-value 1000、tabular 输出含 qseq/sseq、DUST off),与同一基因组数据库比对;
2. HSP → 结合位点解析(`hits_to_binding_sites`):
   - **全长身份过滤**:引物全长身份比 ≥ `binding_min_identity`(默认 80%)才是有效结合位点;低于 80% 的是低 Tm 无效短命中(如 20 bp 引物只配 11 bp、退火 ~33°C,标准 PCR 退火温度下不结合),丢弃;
   - 3' 端覆盖的 HSP 判定 3' 端错配(结合取向下最后 3 bp 逐位比较);**修剪端判定**:仅当 HSP 从 3' 端边缘开始(plus qstart==1 / minus qend==n,blastn 在 3' 端错配处停止延伸)修剪量才计入 3' 端错配——3' 端完全配对、5' 端错配被修剪的命中(seed 恰在 3' 端)必须保持可延伸;
   - **截断(truncated)**:有效位点数超过 `prefilter_max_hits`(默认 200)时无法完整评估 → 独立档位"命中数超上限(无法完整评估,淘汰)"直接淘汰(在身份过滤后按有效位点计数判定,原始 HSP 数在真实大库动辄数百不构成截断);
3. 按**3' 端特异性法则**对每对候选进行过滤与优先级排序:

   - **第一优先级(基因组唯一匹配)**:引物除目标位点(模板对应坐标 ±`target_buffer`)外,无任何其他有效结合位点;
   - **第二优先级(3' 端错配豁免)**:引物在非目标位点存在命中,但该命中的**最后 1~3 bp(3' 端)存在错配**(依据比对文本逐位判定;错配落在引物 3' 端 3 bp 内即豁免);
   - **第三优先级(不可成对扩增)**:两条引物各自存在非目标命中,但在任何非目标库条目上,正向与反向引物**无法形成有效扩增产物对**(判定规则:同一条库条目上正/反向位点需呈相向方向(即 PCR 取向)、产物长度在 50~4000 bp 范围内;凡不满足即为"无法成对扩增",记为安全);
   - **淘汰**:命中满足"3' 端最后 ≥3 bp 完全配对、且与另一条引物在同一区域可形成有效扩增产物对"的候选直接淘汰;若全部候选被淘汰,设计失败,给出失败诊断(重复度最高的区间列表、建议放宽的参数)。
4. 对幸存候选,计算每条的脱靶扩增位点数(非目标库条目上可成对形成 50~4000 bp 产物的位置数)。

### 7.5 汇总输出与综合评分

最终输出引物列表(每行一条引物对),每项包含:

- **物理参数**:上游/下游引物序列(5'→3')、长度、Tm、GC%、产物长度、primer3 惩罚分、发夹/二聚体风险;
- **基因组特异性评估**:特异性等级(唯一匹配 / 3' 端错配豁免 / 不可成对扩增)、脱靶扩增位点数、命中详情(可展开到 HSP 级);
- **位置**:模板坐标与基因组绝对坐标(含链方向),目标区覆盖情况;
- **综合评分(0~100)**:由物理分与特异性分加权(权重可配置 `score_physical_weight` / `score_specificity_weight`,默认各 0.5):
  - 物理分:由 primer3 惩罚分归一(惩罚分越低分越高),并叠加 Tm 接近最优、GC 落在 40%~60% 区间、无发夹/二聚体的加减分;
  - 特异性分:唯一匹配 100 分;3' 端错配豁免 80 分;不可成对扩增 60 分;基于 k-mer 深度(跳过特异性查询时)按 3' 端剖面折算;命中数超上限(truncated)0 分;
  - 脱靶扩增位点数每多一个扣分(上限扣完该项)。
- 按综合评分降序排列,并列前可按下标排序;
- 全部证据(比对深度、各阶段屏蔽、blastn-short 命中)随结果一起进入可视化模块(见第 8 章)。

### 7.6 设计参数表(默认值)

| 参数 | 默认 | 说明 |
|---|---|---|
| Tm 范围(最小/最优/最大) | 55 / 60 / 65 °C | primer3 的最优/最小/最大 Tm |
| GC 范围 | 30% ~ 70% | primer3 约束 |
| 引物长度范围 | 18 ~ 25 bp | primer3 约束 |
| 产物长度范围 | 150 ~ 300 bp | 支持"绝对值"或"相对序列长度";另有"不限制"模式(单引物) |
| 侧翼延伸长度 | 150 bp | 目标区两侧可用模板的延伸长度(不跨库条目/染色体) |
| 最大引物二聚体 | 5 bp | 引物对相互二聚体阈值(primer3 或独立校验) |
| 最大 Tm 差(上下游) | 2 °C | |
| 3' 端 5 bp 最大 GC 数 | 3 | |
| 可用区两侧缓冲 | 8 bp | 每个连续可用区上下游各外扩的设计允许带(方向性:正向左扩/反向右扩,3' 端约束回可用区,外扩裁剪到 F/R 范围) |
| 结合位点最小身份比 | 80% | 低于此的全长身份命中是低 Tm 无效短命中,不计位点 |
| 命中数上限(prefilter) | 200 | 有效位点超此数 → truncated 独立档位淘汰 |
| 候选池上限(每级) | 50 对 | candidate_count,坐标多样性均衡 |
| 阶段二放行阈值(global) | 0.6934 | level2_global_th:L2 的 global/3' 阈值(count 2-3 边界),可视化色带/参考线/图例跟随 |
| 阶段三放行阈值(global) | 0.5503 | level3_global_th:L3 的 global/3' 阈值(count 4-6 边界),同上 |
| 非目标扩增产物判定 | 50~4000 bp | |
| 目标区内命中判定缓冲 | ±50 bp | 仅评估判定:target_loci 命中归属与脱靶判定的坐标容差(不参与引物几何设计) |
| 跳过特异性查询 | 关 | skip_spec_eval:不做 blastn-short 逆向验证,特异性分按 k-mer 剖面 |
| 跳过 k-mer 评分过程 | 关 | skip_kmer_scoring:不建深度/剖面,四段式按全放行,不查/不写 k-mer 缓存 |

- 所有参数随配置持久化(下次打开沿用)。
- 界面提供"恢复默认"按钮。
- 产物长度"不限制"模式下,设计**单条引物**(上游/下游独立列表),不要求成对产物——此时不进行"成对扩增"判定,特异性按单引物 3' 端法则评估。

### 7.7 设计执行与任务管理

- 点击"开始设计"后为后台任务;界面显示当前阶段步骤条(第一步比对 → k-mer 计数 → 剖面 → 分级设计 → 引物生成 → 特异性验证),各阶段日志实时可见;底部状态栏文字带实时百分比;
- 可随时"停止",终止 BLAST/primer3 任务并清理临时文件;
- 单查询耗时上限可配置(默认 600 秒),超时给出"建议放宽哪些参数"的提示;
- 支持一次输入多条模板序列(多 FASTA),逐条执行并在结果中按查询分组;任一条失败不影响其他条。

### 7.8 设计结果查看与导出

结果按"查询 → 引物对"树展示,列:Tm(F/R)、GC(F/R)、二聚体/脱靶数、产物长度、综合评分;展开每条引物对:

- 详情面板:F/R 序列、长度、Tm、GC、绝对坐标、产物长度、二聚体/发夹信息、脱靶位点数与位点列表;
- 特异性命中列表:每条引物的 blastn-short 命中(可展开比对详情);
- 导出:
  - **引物序列 .fasta**(`>Q_<query>_Pair_<n>_F len= tm= gc=%` 头,60 字符换行;单引物/ sgRNA 模式同样导出);
  - **引物统计表 .csv**(表头见 9.5);
  - 一键复制全部引物序列到剪贴板。

### 7.9 附加模式

为完整保留既有能力,在标准引物对之外提供两种附加模式(下拉选择,默认"标准引物对"):

- **sgRNA 设计**:20 bp 引导序列(`sgrna_len`,可配置)+ 3' 端 PAM(`sgrna_pam`,默认 NGG,可配置),两条链均扫描;物理约束简化为 GC 40%~60%(硬编码)、长度 20 bp;**同样参与 k-mer 评分与四段式分级**——guide 3' 端位置必须落在该级允许区(global/three_prime ≥ 阈值)内,逐级放宽,有产出即成功;跳过特异性查询时按 3' 端剖面评分(profiles["three_prime"]×100);特异性评估复用 7.4 的 blastn-short 法则(按单引物处理);输出 PAM、链方向、脱靶数。
- **单引物(不限制产物)**:见 7.6 中"不限制"模式;输出**正向与反向两套独立引物**(各带 F/R 方向标记,互不要求成对扩增)。

### 7.10 名称型模板与侧翼滑块

**名称型 query 语法**(模板文本框输入,500ms 防抖自动解析,无需独立"定位"步骤):

```
>条目名[,range=起-止][,database=库名][,name=显示名][,targetbase=库名|False]
```

- `range` 缺省 = 条目全长;`name=` 为显示标签(仅用于结果摘要头与导出,信息卡仍显示真实条目名);`targetbase=` 指定特异性比对库,`False` 关闭特异性比对;
- 解析成功后按 `[range−flank, range+flank]` 从**同一库条目内部**截取侧翼模板(不跨条目、边界截短时显示实际提取范围),`dp-flank` 输入框位于侧翼模板卡内,修改后自动重新解析。

**F/R 设计范围滑块**(v2 帧,**两轴同原点**):

- F 轴坐标 `0` = 目标区间起点(上游侧翼为负);R 轴坐标 `0` = 目标区间起点、`L−1` = 目标区间终点(0 基);
- 显示记法 `*N` = 距端点 N bp、`*0` = 恰在端点;滑块拖动自动钳制在轴边界内,手动输入越界/越序即时红框拦截;
- 滑块值 → 模板 1-based 坐标:`left_range = [a+f0+1, a+f1+1]`、`right_range = [a+r0+1, a+r1+1]`(a = target.start−1);
- 设计范围独立持久化(serializeNamed v:2,切页/刷新/修改名称型输入后恢复;新模板滑块范围外则回默认);
- 模板坐标 ↔ 基因组绝对坐标:`abs = extract_start + t − 1`(正链;负链仅后端能力,UI 名称型路径恒为正链)。

### 7.11 k-mer 缓存与结果持久化

- **k-mer 结果缓存**:键 = sha256(`seq \0 depth_db \0 mode \0 "(8, 10, 12, 15)"`)——F/R 范围不入键;mode 归一化为 `sgrna` / `pair`(standard 与 single 共享缓存);
- 缓存携带自描述元数据 `db`/`gene`/`seq`/`mode`/`detected_ranges`(实际检测的 1-based 区间,排除 F/R 均不设计而未检测的区域),复用前按覆盖检查——范围变化只重算未覆盖区(部分命中),flank 变化导致模板嵌套时按子串偏移映射复用(子串命中),未覆盖则整体重算;
- 前端 sessionStorage 前缀 `bp_kmer_cache:`,高级参数区提供"清空 k-mer 缓存"按钮与条数显示;项目保存随结果写入、加载回灌;
- **结果持久化**:设计/比对结果 localStorage → IndexedDB 两级回退(溢出自动降级),结果 JSON 中的 `step1_hsps` 只保留轻量坐标字段(大命中时全文可达数百 MB)。

---

## 8. 引物设计的可视化联动

### 8.1 设计前/设计中的基因组证据视图

设计页在**设计可视化**(单 canvas)中呈现模板的基因组证据与引物落点:

- **特异性剖面色带**:X 轴为模板坐标,每个碱基位置按 count 分档着色(≥1.0(count=1)绿/L1 / ≥0.6934(count 2-3)黄/L2 / ≥0.5503(count 4-6)橙/L3 / <0.5503(count≥7)红 / 设计范围外灰标);F/R 设计范围外区域以专属灰色标注(图例含"设计范围外(正/反向均不可设计)");
- **global / 3' 双曲线**:全局剖面实线 + 3' 端剖面虚线(分数 0~1),count 分档参考线 1.0/0.6934/0.5503;
- **目标区域蓝框**:目标区以半透明蓝填充 + 加粗(2.5px)蓝框描边;
- **引物落点**:幸存引物对以倒三角标记(F 红、R 品红)绘制在色带上,点击结果表行联动高亮;
- **图例**:可用/低特异区(k-mer 分数区间)/屏蔽/目标区域/设计范围外/正反向引物标记;L2/L3/屏蔽三档的阈值文字随设计实际使用的"阶段二/三放行阈值"动态渲染(非固定值)。

### 8.2 运行中状态

- 运行中显示**步骤条胶囊**(第一步比对 → k-mer 计数 → 深度剖面 → 分级设计 → 引物生成 → 特异性验证,✓ 完成/spinner 当前/灰待办)与秒表;底部状态栏文字带实时百分比;
- 无实时阶段演进动画(每级屏蔽/放行的过程不可见,只有最终剖面与引物落点)。

### 8.3 结果联动

设计完成后:

- 结果表、可视化色带、详情面板三处联动(点击结果行 → 色带标记高亮 + 详情面板;点击详情中的命中/脱靶产物 → 位点列表展开);
- blastn-short 命中与脱靶扩增产物在详情面板以列表展示(不绘制在色带上);
- 无 SVG/PNG 整页导出功能。

---

## 9. 文件读写格式规范

### 9.1 FASTA(输入)

- 数据库输入:一条或多条记录;标题行 `>id description...`;序列行可换行、可含小写;
- 查询输入:同上;亦接受无 `>` 头的纯序列(提交时自动补 `>query`);
- 数据库构建与 BLAST 查询均使用 Biopython 兼容解析(自动忽略空行;`*`、`-`、N、U 合法);
- 序列类型自动检测:取前 2000 个碱基样本,ACGTUN 占比 ≥90% 判核酸,否则判蛋白。

### 9.2 名称型 query 语法

```
>条目名[,range=起-止][,database=库名]
```

- `range` 边界支持数字与 `*` 通配(如 `100-*` 表示从 100 到末尾,`*-200` 从开头到 200);
- `database` 按历史库记录的名称模糊匹配;
- 解析成功后回填为普通 FASTA(标题为 `>显示名[范围]` 形式,如 `>Bomo_Chr4[100-200]`),携带原始坐标信息。

### 9.3 原始 BLAST 输出(导入/导出)

- 导入必须为 NCBI `-outfmt 0` 成对输出文本(包含 `Query=`),其他格式(如 `-outfmt 6` tabular)导入时提示"需要成对文本格式"并拒绝;
- 程序内部解析该文本为结构化结果(Query → Subject → HSP:score、evalue、identity、坐标、比对文本),解析失败给出错误并建议检查文件;
- 引物设计产生的特异性 BLAST 原始输出(tabular)保存在结果原始输出页,可导出。

### 9.4 项目文件(.json,保存/加载)

保存内容:

```
{
  "app": "BlastPrimeStudio",
  "version": 1,
  "timestamp": "ISO 时间",
  "kind": "blast" | "primer_design",     # 项目类型
  "raw_output": "...",                    # -outfmt 0 原文(比对项目)
  "parsed": [...],                        # 结构化比对结果
  "db_prefix": "...", "db_type": "nucl|prot",
  "options": {
    "query_text": "...", "query_file": "...",
    "query_type": "auto|nucl|prot", "program": "...",
    "evalue": "...", "max_targets": "...", "matrix": "...",
    "extra_args": "...", "short_seq_mode": bool, "remote": bool, "remote_db": "..."
  },
  "primer_design": {                       # 引物设计项目时存在
    "template": "...", "params": {...}, "db": "...",
    "mask_profile": [...],                # 各阶段屏蔽/放行区间
    "depth_profile": [...],               # 逐碱基深度(抽样存储)
    "results": [...]                      # 引物对结果(含全部指标与特异性命中)
  }
}
```

加载规则:校验 `app` 与 `version`;恢复查询文本框、全部参数、数据库选择、结果与可视化状态;文件损坏或版本不符时给出明确错误,不崩溃。

### 9.5 导出格式

**BLAST 统计表 .csv**(UTF-8 with BOM):

```
Query_ID, Subject_ID, Identity, E-value, Bit_Score, Subject_Length, Query_Start, Query_End
```

**引物统计表 .csv**:

```
Query_ID, Pair_Index, Forward_Seq, Forward_Len, Forward_Tm, Forward_GC, Forward_Start, Forward_End,
Reverse_Seq, Reverse_Len, Reverse_Tm, Reverse_GC, Reverse_Start, Reverse_End,
Product_Len, Max_Dimer_Consec, Max_Dimer_Total, Off_Target_Sites, Specificity_Level, Composite_Score
```

(单引物模式:`Direction` 列替代 F/R;sgRNA 模式含 `sgRNA_Seq, PAM, Strand, GC, Tm, Off_Target_Sites`。)

**命中序列 .fasta**:标题 `>subject_id_hspN [from_query:xxx score:xxx]`,每 60 字符换行。

**引物序列 .fasta**:标题 `>Q_<query>_Pair_<n>_F len=xx tm=xx.x gc=xx.x%`(R 同理;sgRNA/单引物模式标题对应标注)。

### 9.6 配置与数据目录

- 程序数据目录:`local_blast_dbs/`(随项目),内含 `.gitignore`(写入 `*`)与 `config.json`。
- 数据库历史记录存于配置;引物设计参数(见 7.6,含模式)持久化于配置;下次打开沿用。
- BLAST 工具手动指定路径、语言、主题、日志级别/文件路径同样持久化。

---

## 10. 配置与状态持久化

`config.json` 结构:

```
{
  "lang": "zh" | "en",
  "theme": "system" | "light" | "dark",
  "loglevel": "INFO",
  "logfile": "",
  "blast_bin_dir": "",            # 手动指定的 BLAST 目录
  "db_records": [ {"prefix": "...", "is_created": true} ],
  "primer_params": { ... }        # 7.6 全部参数
}
```

- 配置损坏时:备份原文件为 `config.json.bak` 并以默认配置启动,状态栏提示;
- 载入配置时自动清理失效的数据库历史记录(索引文件不存在的条目),并即时重写配置文件。

---

## 11. 打包与分发规范(PyInstaller)

### 11.1 目标形态

- Windows:单目录/单文件双击即用的 `.exe`,**内置 NCBI BLAST+**(`bin/` 下全部 Windows 版可执行文件与 DLL),普通用户无需自行安装任何东西。
- 附带说明:`README`(含使用、防火墙说明、数据目录说明)。

### 11.2 打包要求

- 打包时把 `bin/`(BLAST+ 全套,Windows 版)作为数据文件加入;
- 启动后 BLAST 程序定位首先查找 `sys._MEIPASS/bin/`(onefile 解包目录)或 exe 同级 `bin/`,保证内置优先;
- 前端 `static/` 完整打入包内(路径经 `sys._MEIPASS` 解析),不可依赖外部资源;
- 打包后必须验证的核心用例:
  1. 全新机器(无 Python、无 BLAST)双击启动 → 自动开浏览器 → 建库 → BLAST → 可视化;
  2. 引物设计四段式全流程可用;
  3. 杀毒软件不误报(建议签名);
  4. 中文路径、含空格路径下运行正常;
- 打包产物命名:`BlastPrimeStudio-<版本>-win64(.exe)`,版本号遵循语义化版本。

### 11.3 数据目录说明

- 打包模式下,`local_blast_dbs/` 建在 exe 同级的可写目录(如 `%APPDATA%/BlastPrimeStudio/` 或 exe 同级),确保用户有写权限;路径显示在"设置"页,可一键打开。

---

## 12. 错误处理与用户提示规范

| 场景 | 提示方式 | 提示内容要点 |
|---|---|---|
| BLAST 工具缺失 | 顶部横幅 + 按钮置灰 | 缺失工具列表 + 安装说明 + 手动指定入口 |
| 数据库文件不存在/已损坏 | 错误弹窗 | 完整路径、缺失的具体索引文件 |
| FASTA 非法 | 输入框红框 + 错误弹窗 | 定位到非法行/字符,建议"序列清洗" |
| 建库失败 | 错误弹窗 | makeblastdb 原始 stderr 全文 |
| BLAST 无结果 | 结果页占位提示 | "未找到匹配" + 建议放宽 E-value / 检查数据库 |
| 短序列未开短模式 | 确认弹窗 | 警告与继续选项 |
| 远程比对 | 确认弹窗 | 数据将离开本机、可能耗时数小时 |
| 引物设计失败 | 结果页 + 失败诊断 | 失败于哪个阶段、重复度统计、建议放宽的参数 |
| 引物设计超时 | 提示 | 建议收窄产物/Tm 范围或放宽约束 |
| 任务取消 | 状态栏 | "已取消",残留进程已清理 |
| 配置损坏 | 状态栏 + 备份 | 已备份原文件并恢复默认 |

- 所有长任务执行中,页面允许用户离开并随时返回查看进度;
- 所有弹窗文案双语,与界面语言一致。

---

## 13. 验收清单

### A. 通用
- [ ] `uv sync && uv run blastprime` 源码启动成功;桌面系统自动打开浏览器
- [ ] Windows 打包 exe 在无 Python/无 BLAST 环境双击可用,内置 BLAST 生效
- [ ] 源码模式下 BLAST 定位顺序(根目录 → bin/ → PATH)生效;缺失时横幅+手动指定+安装指引可用
- [ ] 中英双语即时切换;浅/深主题;设置持久化
- [ ] 后台任务可取消、进度实时推送、日志抽屉可用

### B. 数据库
- [ ] 多文件合并建库、自动类型检测、默认目录、指定目录、库详情
- [ ] 历史记录:切换、浏览已有、仅删记录、物理删除、失效清理

### C. BLAST 可视化(独立)
- [ ] 参数齐全;程序自动选择正确;短序列模式自动切换 blastn-short
- [ ] 名称型 query(含 range/database/通配)解析正确
- [ ] 结果树排序/过滤;图形摘要缩放/悬停/联动;比对高亮;原始输出懒加载
- [ ] 导入原始输出可完整重建视图
- [ ] 四种导出格式与表头符合 9.5;项目保存/加载往返一致

### D. 引物分析
- [ ] 短序列模式下自动出现;六项指标与判定等级正确;特异性规则与 3' 端偏移/错配修正一致

### E. 引物设计(新流程)
- [ ] 输入(序列 / 名称+范围)+ 基因组库必选;目标区域可选
- [ ] 第一步 blastn 全序列比对 + k-mer 计数逐碱基深度分析 + 特异性剖面可视化
- [ ] 四段式 count 分档阈值(1.0/0.6934/0.5503/全放行,对应 count=1/2-3/4-6/≥7)逐级放宽,有产出即停;可用区两侧缓冲(默认 8 bp,方向性扩展、3' 端回约束、范围裁剪)生效
- [ ] blastn-short 逆向评估:binding_min_identity 80% 身份过滤、truncated 独立档位、3' 端三级排序与淘汰
- [ ] 综合评分(物理/特异权重默认各 50%,可配置)计算与排序正确
- [ ] 引物位置/剖面/结果同步推送可视化;结果表/色带/详情三处联动
- [ ] 跳过特异性查询 / 跳过 k-mer 评分过程 两开关行为正确;sgRNA(四段式)与单引物(不限制产物)模式可用
- [ ] 导出:引物 .fasta、统计 .csv;参数持久化;结果切页保留(k-mer 缓存复用/清空)

---

*本规范文档为 BlastPrime Studio 的完整功能规格。*
